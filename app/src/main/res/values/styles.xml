<resources>

    <!-- Base application theme. -->
    <style name="AppThemeDark" parent="Theme.AppCompat">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="android:windowBackground">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccentDark</item>
        <item name="android:textColor">@color/textColorPrimary</item>
        <item name="android:textStyle">bold</item>
    </style>

    <style name="AppThemeLight" parent="Theme.AppCompat.Light">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/titleColor</item> <!-- kolor z nazwa aplikacji -->
        <item name="colorPrimaryDark">@color/titleColor</item> <!-- kolor z godzina itp -->
        <item name="android:windowBackground">@color/textColorPrimary</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:textColor">@color/textColorSecondary</item> <!-- tekst -->
    </style>

    <style name="BottomNavigationTheme">
        <item name="android:tint">@color/navigationTintColor</item>
    </style>
</resources>
